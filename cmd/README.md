The `cmd` folder is where we can compose the logical groupings (Adapter (like storage, http, gRPC) & 
Domain (core business logic)) into practical applications, which have black-box tests to verify it all works.
